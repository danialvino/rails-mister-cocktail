<h1><%= @cocktail.name %> </h1>

<% @doses.each do |dose| %>
<h6> <%= dose.description %> of <%=dose.ingredient.name %> <%= link_to 'Destroy', dose_path(dose), method: :delete, data: { confirm: 'Are you sure?' } %>  </h6>
<% end %>
<p> <%= link_to 'Add a dose', new_cocktail_dose_path(@cocktail) %> </p>
<p> <%= link_to 'See all doses', cocktails_path %> </p>
